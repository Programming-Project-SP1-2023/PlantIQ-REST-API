package com.plantiq.plantiqserver.service;

import org.junit.jupiter.api.Test;

class SessionServiceTest {
	//	Blanked out due to database connection issues
	@Test
	public void testCreate() {
		// Mock the required dependencies and setup initial conditions
//		String userId = "123";
//		ArrayList<Session> mockSessions = new ArrayList<>();
//		Session mockSession = Mockito.mock(Session.class);
//		mockSessions.add(mockSession);
//		Session.setMockedSessions(mockSessions);
//		Mockito.when(mockSession.getUserId()).thenReturn(userId);
//
//		// Mock the Session.insert method
//		Mockito.when(Session.insert(Mockito.anyString(), Mockito.any(HashMap.class))).thenReturn(true);
//
//		// Call the create method
//		String token = SessionService.create(userId);
//
//		// Assertions and verifications
//		Assertions.assertNotNull(token);
//		Assertions.assertNotEquals("", token);
//		Assertions.assertEquals(1, mockSessions.size());
//		Assertions.assertEquals(userId, mockSession.getUserId());
//		Assertions.assertEquals(token, mockSession.getToken());
//
//		// Verify the mock interactions
//		Mockito.verify(mockSession, Mockito.times(1)).delete("token");
//		Mockito.verify(mockSession, Mockito.times(1)).setToken(Mockito.eq(token));
	}
}