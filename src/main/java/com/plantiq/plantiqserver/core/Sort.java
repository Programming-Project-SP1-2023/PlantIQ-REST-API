package com.plantiq.plantiqserver.core;


//|================================================|
//|                   SORT CLASS                   |
//|================================================|


//Enum used when ordering record of a database. The records can be ordered in either
public enum Sort {
    ASC,
    DESC
}